<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 64 64">
  <defs>
    <radialGradient id="glow" cx="80%" cy="20%" r="50%">
      <stop offset="0" stop-color="#d4af37" stop-opacity=".25"/>
      <stop offset="1" stop-color="#d4af37" stop-opacity="0"/>
    </radialGradient>
  </defs>

  <!-- fond -->
  <rect width="64" height="64" rx="12" fill="#0b0e13"/>
  <!-- halo doré -->
  <circle cx="50" cy="14" r="18" fill="url(#glow)"/>

  <!-- monogramme -->
  <text x="50%" y="50%" dy=".36em"
        text-anchor="middle"
        font-family="Space Grotesk, Inter, system-ui, -apple-system, Segoe UI, Roboto, Arial"
        font-weight="700" font-size="28" fill="#d4af37">SC</text>
</svg>
